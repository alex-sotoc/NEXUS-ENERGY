class AppConstants { 
  AppConstants._(); 
  static const String realDeviceId = 'ESP32_RELAY_01'; 
  static const Duration realtimeRefreshInterval = Duration(seconds: 3);
  static const Duration requestTimeout = Duration(seconds: 8); 
  }